import { render, screen } from '@testing-library/react';
import App from './App';

test('renders Ashoka\'s SuperBazaar header', () => {
  render(<App />);
//   const linkElement = screen.getByText(/Ashoka's SuperBazaar/i);
//   expect(linkElement).toBeInTheDocument();
});